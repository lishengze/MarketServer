#include "kafka_quote.h"
#include "../Log/log.h"


KafkaQuote::KafkaQuote(string server_address)
{
    
}

void KafkaQuote::start_listen_data()
{
    try
    {
        /* code */
    }
    catch(const std::exception& e)
    {
        LOG_ERROR(e.what());
    }
}

void KafkaQuote::listen_data_main()
{
    try
    {
        /* code */
    }
    catch(const std::exception& e)
    {
        LOG_ERROR(e.what());
    }
}